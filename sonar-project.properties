# Monorepo Configuration
# Coverage starts low (~30%) as only shared-core has tests currently.
# Coverage will automatically improve as tests are added to api-core and frontend-core.
# Quality gate failures are non-blocking during initial development.

sonar.projectKey=jeffcaradona_animated-spork
sonar.organization=xbe90seup2ltm6gwmugkq

# Path to sources (all 3 core packages)
sonar.sources=packages/shared-core/src,packages/api-core/src,packages/frontend-core/src

# Exclude test files and node_modules from analysis
sonar.exclusions=**/tests/**,**/node_modules/**

# Path to tests (only shared-core has tests currently)
sonar.tests=packages/shared-core/tests

# Coverage report (LCOV format from shared-core only for now)
sonar.javascript.lcov.reportPaths=packages/shared-core/coverage/lcov.info

# Exclude untested packages from NEW code coverage requirements (Optional)
# This prevents quality gate from failing on new code in packages without tests yet
sonar.coverage.exclusions=packages/api-core/**,packages/frontend-core/**

# Source encoding
sonar.sourceEncoding=UTF-8

